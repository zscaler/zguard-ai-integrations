# Zscaler AI Guard — copy to Cline/.env or repository root .env
AIGUARD_API_KEY=
AIGUARD_CLOUD=us1
# Optional numeric policy ID (uses execute-policy instead of resolve-and-execute-policy)
AIGUARD_POLICY_ID=
AIGUARD_TIMEOUT=30
